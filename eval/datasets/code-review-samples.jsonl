{"code": "function calculateTotal(items) { let total = 0; for (let i = 0; i < items.length; i++) { total = total + items[i].price; } return total; }", "expected": "The function is correct but could be improved. Consider using array.reduce() for better readability and functional programming style. Also, validate that items is an array and handle edge cases like empty arrays or items without price properties."}
{"code": "const fetchUser = async (id) => { const response = await fetch(`/api/users/${id}`); return response.json(); }", "expected": "Missing error handling. Should check response.ok before parsing JSON. Consider adding try-catch for network errors. The endpoint is vulnerable to injection if id is not validated. Add input validation and proper error handling."}
{"code": "class User { constructor(name, email) { this.name = name; this.email = email; } getName() { return this.name; } }", "expected": "Good basic implementation. Consider adding input validation in constructor. Email format should be validated. Consider making properties private using # prefix. Add JSDoc comments for better documentation."}
{"code": "SELECT * FROM users WHERE username = '" + userInput + "'", "expected": "Critical SQL injection vulnerability! Never concatenate user input directly into SQL queries. Use parameterized queries or prepared statements. This is a severe security risk that could allow attackers to access or modify database contents."}
{"code": "const API_KEY = 'sk-1234567890abcdef'; fetch('https://api.example.com/data', { headers: { 'Authorization': API_KEY } });", "expected": "Security issue: hardcoded API key in source code. Move sensitive credentials to environment variables. Never commit secrets to version control. Use process.env.API_KEY and store actual values in .env file."}
